export enum Tolerance {
  Lenient, // an answer that's neither case nor space sensitive; rest are self-explanatory
  CaseSensitive,
  SpaceSensitive,
  CaseAndSpaceSensitive, // this is the default for answers
}
